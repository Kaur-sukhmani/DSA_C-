// factorial 
#include<stdio.h>
int main(){
    int n,result;
    printf("Enter the number for which you want to find the factorial:");
    scanf("%d", &n);
    for(int i=1;i<=n;i++){
       result*=i;
    }
    printf("factorial:%d", result);
    return 0;
}
