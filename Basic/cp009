// reverse the number
#include<stdio.h>
int main(){
    int num;
    printf("Enter the number:");
    scanf("%d",&num);
    for(int i=num;i>0;i/=10){
        int r=i%10;
        printf("%d",r);
    }
    return 0;
}
